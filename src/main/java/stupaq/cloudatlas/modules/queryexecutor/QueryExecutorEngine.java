package stupaq.cloudatlas.modules.queryexecutor;

import stupaq.cloudatlas.attribute.Attribute;
import stupaq.cloudatlas.attribute.AttributeName;
import stupaq.cloudatlas.attribute.types.CAQuery;
import stupaq.cloudatlas.naming.GlobalName;
import stupaq.cloudatlas.zone.ZoneManagementInfo;
import stupaq.cloudatlas.zone.hierarchy.ZoneHierarchy;

class QueryExecutorEngine {
  private void installQuery(CAQuery query) {
  }

  private void removeQuery(AttributeName queryName) {
  }

  private void updateAttribute(GlobalName globalName, Attribute attribute) {
  }

  private void removeAttribute(GlobalName globalName, Attribute attribute) {
  }
}
