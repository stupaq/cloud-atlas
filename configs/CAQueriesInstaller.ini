replace_all = true

[&contacts_election]
query = "SELECT to_set(random(5, unfold(contacts))) AS contacts"

[&num_processes]
query = "SELECT sum(num_processes) AS num_processes"

[&children_count]
zones = /
query = "SELECT count(distinct(name)) AS children_count"
